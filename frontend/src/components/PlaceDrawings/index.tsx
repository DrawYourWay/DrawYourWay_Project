import { GridItem } from "@chakra-ui/react";

interface PlaceDrawingsProps {
  placeId: string;
}

const PlaceDrawings = ({ placeId }: PlaceDrawingsProps) => {
  return (
    <GridItem rowSpan={2} width="100%" height="100%">
      dsa
    </GridItem>
  );
};

export default PlaceDrawings;
